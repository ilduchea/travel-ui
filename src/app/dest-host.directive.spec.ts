import { DestHostDirective } from './dest-host.directive';

describe('DestHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DestHostDirective();
    expect(directive).toBeTruthy();
  });
});
